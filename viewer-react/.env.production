# Render デプロイ用。React の接続先は BFF のみ。
# React → FastAPI BFF（airspace-viewer-api）→ Laravel API（Private Service）
#
# VITE_ 接頭辞の値はブラウザ配布物にそのまま埋め込まれるため、APIキー等の
# 秘密情報はここに置かない。Laravel の X-API-Key は BFF 側（サーバ）で付与する。
VITE_API_BASE=https://airspace-viewer-api.onrender.com
